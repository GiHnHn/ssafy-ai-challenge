# 평가 조건과 결과

## 단일 모델의 학습 전후 비교

| 항목 | 조건 |
|---|---|
| 모델 | `Qwen/Qwen3.5-9B` |
| 검증 데이터 | `train.csv` 마지막 10%, 508문항 |
| 학습용 gold 데이터 | 앞 90%, 4,565문항 |
| 입력 | 512px 이미지와 동일한 질문·선택지 프롬프트 |
| 추론 | 다음 답변 토큰의 a/b/c/d 확률로 정답 선택 |
| 추가 학습 | vision·language LoRA, gold + 선별한 dev 의사 라벨 |
| 체크포인트 선택 | 검증 정답 수 우선, 동률이면 정답의 negative log-likelihood가 낮은 epoch |

원본 검증 출력 `qwen35_zero_valid.csv`, `qwen35_tuned_valid.csv`를 비교했습니다. 두 파일의 문항 ID·순서·이미지 경로·질문·선택지·정답이 모두 일치함을 확인했습니다. 문항별 원본 데이터와 예측은 공개하지 않고 집계 결과만 기록합니다.

| 구분 | 정답 / 전체 | 정답률 |
|---|---:|---:|
| Zero-shot | 429 / 508 | 84.45% |
| LoRA 학습 후 | 474 / 508 | 93.31% |
| 차이 | +45 / 508 | +8.86%p |

학습 후 오답에서 정답으로 바뀐 문항은 57개, 정답에서 오답으로 바뀐 문항은 12개입니다. 정답 수의 순증가는 45개입니다.

**해석 범위:** 체크포인트 선택에 사용한 검증셋에서의 개선입니다. 독립 테스트 성능으로 제시하지 않습니다. 이후 전체 gold 데이터로 재학습한 모델과 최종 앙상블의 성능은 이 비교에 포함하지 않았습니다.

구현은 [512px 학습 노트북](../notebooks/02_Qwen3_5_9B_512_VQA.ipynb)의 데이터 분할, zero-shot 평가, epoch별 검증, 학습 후 평가 셀에서 확인할 수 있습니다.

## 대회 결과

- Public 최고 점수: **0.94994**
- 최종 Private 순위: **5위**
- 근거 범위: 보관된 제출 기록 및 참가자 본인 확인

Public과 Private은 대회 평가이며, 위 508문항 검증 정답률과 평가 대상이 다릅니다. 세 값을 하나의 성능 향상 곡선으로 비교하지 않습니다.

## 최종 제출 재현

공개용으로 정리한 [`ensemble.py`](../ensemble.py)는 보관된 모델 예측과 검색 후보를 입력받아 당시 최종 결합 수식을 재현합니다.

| 확인 항목 | 결과 |
|---|---|
| 제출 문항 수 | 5,074 |
| ID와 순서 | 원본 제출과 전체 일치 |
| 다른 답안 수 | 0 |
| CSV SHA-256 | `3ef45df9b1d67973a379e0b957c06111ccac9a600a8613537b0d9d59e47db300` |

GPU 학습 전체의 재실행이나 대회 서버 점수 재측정은 하지 않았습니다. 같은 예측 파일이 주어졌을 때 최종 답안을 다시 생성할 수 있음을 확인한 결과입니다.

입력 확률 파일과 검색 후보의 구성은 [실행 안내](REPRODUCING.md)에 정리했습니다.
